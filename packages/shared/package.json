{
  "name": "@transitops/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Common types, utilities, and constants for TransitOps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint 'src/**/*.ts'",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
