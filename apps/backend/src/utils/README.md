# Utility Helpers

Place helper functions and local modules here (e.g. cryptography, custom loggers, token generators).
Ensure domain-agnostic generic functions are stored in `packages/shared/src/utils/` instead.
