# Middleware Components

Place Express middleware implementations here:
- Authentication guards (`auth.middleware.ts`)
- Role-based Access Control checks (`rbac.middleware.ts`)
- Request body/query validators using schema engines like Zod
- Rate limiting configurations
