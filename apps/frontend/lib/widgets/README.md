# Global Reusable Presentation Widgets

Place shared, domain-agnostic UI widgets here:
- Custom primary/secondary buttons (`custom_button.dart`)
- Validated text field wrappers (`custom_text_field.dart`)
- Loader overlays and skeleton widgets (`skeleton_loader.dart`)
- Error view blocks and empty-state placeholders (`empty_state.dart`)
